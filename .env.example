# ==============================================================================
# Projet Sentinel — Variables d'Environnement
# Copiez ce fichier en .env et renseignez VOS valeurs RÉELLES.
# Ne committez JAMAIS le fichier .env dans Git.
# ==============================================================================

# ------------------------------------------------------------------------------
# Neo4j
# ------------------------------------------------------------------------------
NEO4J_PASSWORD=ChangeMe_SuperSecretNeo4j!

# ------------------------------------------------------------------------------
# JWT — Sécurité des tokens d'authentification
# Générez une clé forte : python -c "import secrets; print(secrets.token_hex(64))"
# ------------------------------------------------------------------------------
JWT_SECRET_KEY=replace_me_with_a_very_long_random_secret_key_at_least_64_chars
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=480

# ------------------------------------------------------------------------------
# Compte ADMIN initial (créé automatiquement au 1er démarrage du backend)
# ------------------------------------------------------------------------------
ADMIN_EMAIL=admin@sentinel.local
ADMIN_PASSWORD=ChangeMe_Admin123!
ADMIN_NOM=Administrateur

# ------------------------------------------------------------------------------
# Brevo SMTP (ex-Sendinblue) — Service d'e-mails transactionnels
# Récupérez votre clé API sur https://app.brevo.com → SMTP & API
# ------------------------------------------------------------------------------
BREVO_API_KEY=xkeysib-replace_with_your_brevo_api_key
BREVO_SENDER_EMAIL=noreply@sentinel.local
BREVO_SENDER_NAME=Sentinel App

# ------------------------------------------------------------------------------
# Gemini AI
# ------------------------------------------------------------------------------
GEMINI_API_KEY=AIzaSy_replace_with_your_gemini_api_key

# ------------------------------------------------------------------------------
# GitHub Agent Integration
# ------------------------------------------------------------------------------
GITHUB_TOKEN=github_pat_replace_with_fine_grained_pat
GITHUB_REPO=username/repository

# ------------------------------------------------------------------------------
# Discord Bot Worker (Optionnel)
# ------------------------------------------------------------------------------
DISCORD_BOT_TOKEN=replace_with_discord_bot_token
DISCORD_RATE_LIMIT_RPM=5

# ------------------------------------------------------------------------------
# Application
# ------------------------------------------------------------------------------
APP_ENV=production
APP_DEBUG=false
CORS_ORIGINS=http://localhost:9080,http://127.0.0.1:9080

